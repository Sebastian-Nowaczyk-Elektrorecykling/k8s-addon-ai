apiVersion: apps/v1
kind: Deployment
metadata:
  name: litellm
  namespace: ai-system
spec:
  template:
    spec:
      containers:
      - name: litellm
        env:
        - name: VLLM_API_KEY
          valueFrom:
            secretKeyRef:
              name: vllm-auth
              key: api-key
